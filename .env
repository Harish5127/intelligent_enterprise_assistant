# Copy this file to .env and fill values for SMTP (used for sending OTPs)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-username
SMTP_PASS=your-smtp-password
FROM_EMAIL=bot@example.com
